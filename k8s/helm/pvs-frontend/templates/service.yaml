apiVersion: v1
kind: Service
metadata:
  name: {{ include "pvs-frontend.fullname" . }}
  labels:
    app: {{ include "pvs-frontend.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    app: {{ include "pvs-frontend.name" . }}
